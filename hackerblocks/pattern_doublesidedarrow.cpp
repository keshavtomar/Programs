#include <iostream>
#include <algorithm>

using namespace std;

int main(void)
{
    int n;
    cin >> n;
    int mid = n / 2 + 1;
    int spaces = (mid - 1) * 2;
    int mid_spaces=-1;
    int value=1;
    bool reverse=0;
    while(value>0){

    }
}